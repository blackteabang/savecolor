# Express + MySQL (optional local/tunnel mode)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=dareum
PORT=7749

# Shared by Express and Cloudflare Pages
ADMIN_ID=admin
ADMIN_PW=
ADMIN_TOKEN=
BASE_SIGNATURES=50

# Cloudflare Pages: set the same ADMIN_* / BASE_SIGNATURES via
#   npx wrangler pages secret put <NAME> --project-name=savecolor
# D1 binding is configured in wrangler.toml (DB).
